_schema-version: "3.2"
ID: comtestparentapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comtestparentapp-destination-content
  type: com.sap.application.content
  requires:
  - name: comtestparentapp-destination-service
    parameters:
      content-target: true
  - name: comtestparentapp-repo-host
    parameters:
      service-key:
        name: comtestparentapp-repo-host-key
  - name: comtestparentapp-uaa
    parameters:
      service-key:
        name: comtestparentapp-uaa-key
  parameters:
    content:
      subaccount:
        destinations:
        - Name: comtestparentapp_html_repo_host
          ServiceInstanceName: comtestparentapp-html5-srv
          ServiceKeyName: comtestparentapp-repo-host-key
          sap.cloud.service: comtestparentapp
        - Authentication: OAuth2UserTokenExchange
          Name: comtestparentapp_uaa
          ServiceInstanceName: comtestparentapp-xsuaa-srv
          ServiceKeyName: comtestparentapp-uaa-key
          sap.cloud.service: comtestparentapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comtestparentapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comtestparentapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comtestparentapp.zip
      name: comtestparentapp
      target-path: resources/
- name: comtestparentapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comtestparentapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comtestparentapp-destination-service
    service-plan: lite
- name: comtestparentapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comtestparentapp-xsuaa-srv
    service-plan: application
- name: comtestparentapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comtestparentapp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
